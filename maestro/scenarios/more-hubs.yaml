# More hubs: athlete profile, Settings, health source — assumes authenticated session.
appId: com.coachwatts.app
---
- runFlow: ../subflows/tap-tab-more.yaml
- tapOn:
    id: more-athlete-profile
- extendedWaitUntil:
    visible:
      id: athlete-screen
    timeout: 20000
- runFlow: ../subflows/tap-tab-more.yaml
- scrollUntilVisible:
    element:
      id: more-settings
    direction: DOWN
    timeout: 15000
- tapOn:
    id: more-settings
- extendedWaitUntil:
    visible:
      id: settings-screen
    timeout: 15000
- assertVisible:
    id: more-health-sync

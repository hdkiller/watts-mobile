# Authenticated shell smoke — isolated wipe + login (CI / `pnpm test:e2e:shell`).
# Full suite uses suite-shared.yaml instead (shell is the first shared scenario).
appId: com.coachwatts.app
tags:
  - smoke
  - standalone
---
- runFlow: subflows/boot-and-login.yaml
- runFlow: scenarios/shell-tabs.yaml

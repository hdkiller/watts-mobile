# 004 — Modify/Rest affordance: "Discuss with Coach"

**Area:** Today · **Priority:** medium · **Effort:** M · **Status:** done

## Problem
The product baseline calls for accept / modify / rest, but only Accept exists on Today.

## Proposal
Add a secondary "Discuss with Coach" action on the recommendation hero that opens the Coach tab pre-seeded with today's recommendation context (reuse `src/features/coach/seedContext.ts`). No new API surface needed.

## Done
- Secondary `Button` ("Discuss with Coach") on the Today recommendation CTA block.
- Navigates to `/(app)/(tabs)/coach?discuss=1`.
- Coach auto-starts (new empty room if needed) with `DISCUSS_TODAY_PROMPT`; first send prefixes today/recovery via `buildCoachSeedContext`.

# 016 — Notification preferences screen

**Area:** More · **Priority:** high · **Effort:** M · **Status:** done

## Problem
Baseline "account glue" includes notification prefs; the app has an inbox but no way to control pushes. Matters for store review too.

## Proposal
New `app/(app)/notification-settings.tsx` reached from More: per-event-type toggles (`RECOMMENDATION_READY`, `WORKOUT_ANALYSIS_READY`, `SYNC_COMPLETED`, `COACH_MESSAGE`). Needs a prefs read/write contract with the backend (extend `/api/mobile/devices` or a prefs endpoint) — coordinate with coach-wattz.

# 005 — Recovery glance polish: sentiment dots + friendlier band header

**Area:** Today · **Priority:** medium · **Effort:** S · **Status:** done

## Problem
Sleep/HRV/Feel tiles are plain text with no good/ok/poor signal. The "Active Recovery Context" section header is internal vocabulary.

## Proposal
- Add a small colored status dot per metric tile when the backend provides a rating (extend `mapTodayPayload` if labels encode sentiment).
- Rename the visible header in `src/features/today/active-recovery-band.tsx` to "Recovery" or "How you're doing"; keep the taxonomy name in code/specs.

## Done
- `mapTodayPayload` maps `recovery_context` and `recovery_analysis`, plus `recoverySentimentFrom*` helpers (unit-tested).
- Sleep/HRV/Feel tiles show a green / amber / red dot when sentiment is known.
- Active recovery band visible header reads "How you're doing" (`ActiveRecoveryBand` component name unchanged).

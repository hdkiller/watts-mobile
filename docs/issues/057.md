# 057 — Offline story is two screens deep; connectivity plumbing is missing

**Area:** offline/caching · **Priority:** high · **Effort:** M · **Status:** done

## Problem
Issue 024 shipped offline for Today + planned detail, but that's where it ends. Concrete gaps:

1. **No connectivity wiring at all.** Nothing hooks TanStack Query's `onlineManager` to NetInfo, and nothing hooks `focusManager` to `AppState`. Consequences: no `refetchOnReconnect` — when signal returns at the trailhead, stale screens stay stale until pull-to-refresh; no refetch-on-foreground — reopening the app after hours shows old data silently (RN has no "window focus" without the AppState hookup).
2. **Persistence allowlist is narrow** ([persist.ts](../../src/query/persist.ts)): only `today` and `activity` upcoming/recent/planned survive a relaunch. Offline, everything else hard-errors or shows empty: Coach chat ("Failed to open Coach chat" — rooms/messages aren't cached at all), notifications inbox, recent wellness, training load (PMC), profile, nutrition, measurements, recovery context.
3. **Cached-with-banner is inconsistent.** `OfflineBanner` (cached + "last updated") renders only on Today and planned detail. The activity *list* screens read from the persisted cache too but show it silently as fresh; activity *detail* isn't persisted at all — tapping a row offline errors after the list "worked".
4. **Mutations fail hard offline.** Check-in save, recovery events, chat sends error immediately with no queue. The wellness check-in is *the* field loop — a morning check-in in a dead zone is simply lost (form content also clobbered on later refetch, see 034).

## Proposal
Phased:

- **P1 (plumbing, small):** wire `onlineManager` ↔ `@react-native-community/netinfo` and `focusManager` ↔ `AppState` in the app root; add `refetchOnReconnect: true` defaults. This alone fixes "stale until pull-to-refresh".
- **P2 (read coverage):** extend `shouldPersistQuery` to chat rooms+messages, notifications inbox, profile, wellness glance, PMC; bump the persister maxAge story accordingly; render `OfflineBanner` from a shared connectivity hook on every list/detail that can show cached data (and persist activity detail so list→detail doesn't cliff).
- **P3 (write resilience, scoped):** offline queue for the wellness check-in only (persist pending payload, flush on reconnect, "Saved offline — will sync" state). Chat/events can keep fail-fast with clear copy for v1.

## Resolution (2026-07-20)
Shipped P1 + P2 + scoped P3:

- **P1:** `src/query/connectivity.ts` wires NetInfo → `onlineManager` and AppState → `focusManager`; QueryClient defaults include `refetchOnReconnect: true` (AuthContext). Added `@react-native-community/netinfo` — **rebuild the native binary** after pull.
- **P2:** `shouldPersistQuery` covers chat rooms/messages (via `setQueryData` bridge in `useCoachChat`), notifications inbox, profile, wellness, PMC, and activity detail (not streams/power-curve). Shared `useOfflineCached` + `OfflineBanner` on Today, planned detail, recent activity, activity detail, upcoming.
- **P3:** Log wellness check-in queues to AsyncStorage when offline; `OfflineWellnessFlush` syncs on reconnect; Log shows “Saved offline — will sync”.

Still out of scope: offline queue for chat sends / recovery events; OfflineBanner on every secondary surface (notifications inbox, athlete profile, etc. — cache is persisted but banner not wired there yet).
